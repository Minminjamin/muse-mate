import React from "react";

const UserProfileCard = () => {
  const userName: string = "";
  const userId: string = "";

  return (
    <div>
      <img />
      <div>
        <span>{userName}</span>
        <span>{userId}</span>
      </div>
    </div>
  );
};

export default UserProfileCard;
